# 制作フロー

```
①骨子 → ②構成案 → ③台本 → ④ElevenLabs
```

同じ文言を複数ファイルへ手で同時編集しない。  
**仕上げの正本は③台本。** 大きな方向転換がない限り、①②へ戻らない。④での修正はなるべく減らす。

---

## 段階一覧

| 順 | 段階 | ファイル | 誰がやる | 依るルール |
|----|------|----------|----------|------------|
| ① | 骨子 | `output/*-source.md` など | **依頼者が提供。** エージェントは整える程度 | （体裁の整理のみ） |
| ② | 構成案 | `output/*-outline.md` | エージェントが作成 | `OUTLINE_GUIDELINES.md` |
| ③ | 台本 | `output/*-script.md` | エージェントが作成。構成案を反映 | `GUIDELINES.md` |
| ④ | ElevenLabs | `output/*-script-for-elevenlabs.txt` | ③から生成。手編集しない | 下記 |

---

## ① 骨子

- 依頼者が提供する（主張・順番・含めたい要素）
- エージェントは、読みやすく整える・欠けを史実で補う程度にとどめる
- 構成案・台本を骨子より先に作らない

---

## ② 構成案

- エージェントが、骨子を踏まえて作成する
- `OUTLINE_GUIDELINES.md` に従う
- 話す順番と、各所で言うべき中身を決める
- 台本の言い回しはここで決めない

---

## ③ 台本

- エージェントが、構成案を反映して作成・推敲する
- `GUIDELINES.md` を守る
- **文言の正本はここだけ**
- 言い回し・接続・用語の修正は、原則すべて③で行う

---

## ④ ElevenLabs

③ができたあと、台本から生成する。

```bash
npm run prepare:elevenlabs -- output/<name>-script.md
```

生成内容：

1. ③のブロック名（`#` / `##` などの見出し）を削除する
2. ブロックとブロックの間に、間を開けるための区切りを入れる

```
ーーーーーー
```

- 出力先は `output/<name>-script-for-elevenlabs.txt` のみ
- **④を手で直さない。** 直したい内容があれば③を直し、再生成する
- ④での修正はなるべく減らす（区切りの都合など、やむを得ない場合のみ）

---

## 推敲時の戻し方

```
大きな方向転換（主張・章立て・順番）？
  ├─ はい → ①または②に戻る → ③へ反映 → ④再生成
  └─ いいえ（言い回し・接続・用語）
        → ③だけ直す → ④再生成
```

- 通常の仕上げは③ベース
- 「ついでに構成案も」「ついでに骨子も」は、方向転換があるとき以外やらない
- ④だけ直して③と食い違わせない

---

## 禁止

- ①②③④へ同じ修正をコピペで同時適用しない
- ElevenLabs用の別名ファイルを増やさない
- ③を飛ばして④だけ書く／④だけ推敲する
